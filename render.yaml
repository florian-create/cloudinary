services:
  - type: web
    name: screenshot-api
    env: python
    buildCommand: pip install -r requirements.txt && pyppeteer-install
    startCommand: gunicorn app:app --timeout 120 --workers 1
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
